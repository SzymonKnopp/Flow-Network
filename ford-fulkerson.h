#pragma once
#include "node.h"


std::vector<pipe*> pathToAugment(Node* source);

// fills flow network with maximum flow
void fordFulkerson(Node* source) {
	while (true) {

	}
}

std::vector<pipe*> pathToAugment(Node* source) {

}
